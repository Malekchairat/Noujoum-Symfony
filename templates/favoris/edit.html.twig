{% extends 'base.html.twig' %}

{% block title %}Add Favori{% endblock %}

{% block body %}
    <h1>Add New Favori</h1>

    {{ form_start(form) }}
        {{ form_row(form.id_produit) }}
        {{ form_row(form.date) }}
        <button class="btn">💾 Save</button>
    {{ form_end(form) }}

    <a href="{{ path('favoris_index') }}">← Back to list</a>
{% endblock %}
