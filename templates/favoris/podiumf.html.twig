{% extends 'backoffice.html.twig' %}

{% block title %}Produits les Plus Populaires{% endblock %}

{% block body %}
    <h1 class="my-4">Podium des Produits les Plus Populaires</h1>

    <div class="mt-4">
        <div class="row">
            {% for produit in topFavoris %}
                <div class="col-md-4 mt-3">
                    <div class="card shadow-sm border-0">
                        <div class="card-header bg-dark text-white text-center">
                            {% if loop.index == 1 %}
                                🥇 1ère place
                            {% elseif loop.index == 2 %}
                                🥈 2ème place
                            {% elseif loop.index == 3 %}
                                🥉 3ème place
                            {% endif %}
                        </div>
                        <img src="{{ asset('uploads/images/' ~ produit.imageName) }}" alt="{{ produit.nom }}" class="card-img-top" />
                        <div class="card-body">
                            <h5 class="card-title">{{ produit.nom }}</h5>
                            <p class="card-text">Nombre de favoris: {{ produit.nbFavoris }}</p>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
